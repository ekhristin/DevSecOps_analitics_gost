# Блок-схема DevSecOps-процесса (текущее состояние)

Диаграмма соответствует контексту компании: выделенный специалист по безопасной разработке при отсутствии регламента; стек C#, JavaScript, SQL; GitLab + Jenkins; SAST; Docker/Kubernetes; облако (AWS/Azure); Terraform и Ansible; дефекты безопасности уходят в технический долг с приоритетом по критичности без фиксированных сроков устранения; не ГИС и не ПО в составе КИИ.

На схеме **[явно отражено требование задания](zadanie.md)**:

- ключевые **этапы CI/CD** в Jenkins (от получения кода до выката);
- **точки внедрения проверок безопасности** (SAST в пайплайне) и **мониторинг** (наблюдаемость со стороны облака и Kubernetes — в контексте отдельный регламент не описан);
- **управление доступом** (GitLab, Jenkins, облако, IaC) и **выявление инцидентов** (в описании компании отдельный регламентированный процесс не приведён — на схеме это отмечено как текущий пробел).

```plantuml
@startuml DevSecOps_current_state
skinparam shadowing false
skinparam defaultFontName sans-serif
skinparam activity {
  BackgroundColor #FBFCFD
  BorderColor #334155
  DiamondBackgroundColor #E2E8F0
}
skinparam note {
  BackgroundColor #FEF9C3
  BorderColor #CA8A04
}

title Текущее состояние процессов безопасной разработки (контекст компании)

|#E8EEF7|Организация безопасности разработки|
start
:**Специалист по безопасной разработке ПО**\nвыделен для организации процессов;
:**Регламентация** процессов\nбезопасной разработки **отсутствует**;

|#E8F5E9|Разработка|
:Проектирование и реализация;\n**языки:** C#, JavaScript, SQL;

|#E3F2FD|Управление исходным кодом|
:Хранение и совместная работа;\n**GitLab** (VCS);

|#FFF3E0|CI/CD: этапы пайплайна (**Jenkins**)|

partition "**Ключевые этапы CI/CD**" {
  :**1. Checkout** — получение кода из **GitLab**;
  :**2. Build** — компиляция, сборка артефактов;
  :**3. Test** — автоматизированные тесты;
  partition "**Точка проверки безопасности**" #FCE4EC {
    :**4. SAST** — статический анализ (**единственная явно\nвнедрённая проверка в контексте**);
  }
  :**5. Container build** — сборка образов **Docker**;
  :**6. Publish / Deploy** — публикация артефактов,\nвыкат в **Kubernetes** / **облако**;
}

|#E0F2F1|Инфраструктура (среда выполнения)|
:Кластер **Kubernetes** и сервисы **AWS / Azure**;
:**IaC:** **Terraform** и **Ansible** — описание и применение конфигурации;

|#E1F5FE|Точки **мониторинга** и наблюдаемости|
:Сигналы **облачных платформ** и **K8s**\n(метрики, логи, алерты провайдера);
note right
  В контексте **не описан** единый
  регламент security-мониторинга
  и корреляции событий
end note

|#EDE7F6|Управление **доступом** (IdAM поверх инструментов)|
:Учётные записи и роли **GitLab**;
:Доступ к **Jenkins** и секретам пайплайна;
:**IAM / роли в облаке**, доступ к состоянию **Terraform**\n(who can apply);

|#FFF8E1|Инциденты: **выявление** и реагирование|
:Источники сигналов: алерты облака, K8s,\nвнутренние обращения;\nПроцесс triage / эскалации / runbooks\n**в контексте не регламентирован**;

|#F3E5F5|Работа с дефектами безопасности|
:Все дефекты безопасности включаются\nв **технический долг**;
:Приоритезация по **критичности**;
:**Сроки исправления** дефектов\nбезопасности **не определены**;

|#ECEFF1|Область применения|
:ПО **не** относится к **ГИС**\nи **не** входит в состав **КИИ**\n(по контексту компании);

stop

@enduml
```

Для получения изображения: откройте блок в [PlantUML](https://plantuml.com/) или используйте расширение PlantUML в IDE / `plantuml` CLI.
